'use client';
import { AnimatePresence, motion } from 'framer-motion';
import { useState } from 'react';
const links = [['ABOUT','/#about'],['MENU','/#menu'],['GALLERY','/#gallery'],['RESERVATION','/reservation']];
export function Navbar() { const [open,setOpen] = useState(false); const close=()=>setOpen(false); return <header className="fixed inset-x-0 top-0 z-50 border-b border-white/20 bg-ink/90 text-white backdrop-blur-md"><nav className="mx-auto flex h-[74px] max-w-[1280px] items-center justify-between px-6"><a className="display text-3xl tracking-[.16em]" href="/">NOIR</a><div className="hidden items-center gap-8 lg:flex">{links.map(([label,href])=><a className="eyebrow text-white/80 transition hover:text-white" href={href} key={label}>{label}</a>)}<a className="button text-white" href="/reservation">Reserve a table</a></div><button onClick={()=>setOpen(!open)} aria-label="Open menu" className="eyebrow lg:hidden">{open ? 'CLOSE' : 'MENU'}</button></nav><AnimatePresence>{open && <motion.div initial={{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden border-t border-white/15 bg-ink lg:hidden"><div className="flex flex-col px-6 py-6">{links.map(([label,href])=><a onClick={close} className="border-b border-white/10 py-5 text-sm tracking-[.2em]" href={href} key={label}>{label}</a>)}<a onClick={close} className="mt-6 button" href="/reservation">Reserve a table</a></div></motion.div>}</AnimatePresence></header>; }
