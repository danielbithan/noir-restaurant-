'use client';
import { FormEvent, useState } from 'react';
import { motion } from 'framer-motion';
export function Reservation(){const [sent,setSent]=useState(false); function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSent(true);e.currentTarget.reset();} return <section id="reservation" className="section bg-ocean text-white"><div className="mx-auto grid max-w-5xl gap-12 lg:grid-cols-2"><div><p className="eyebrow mb-5 text-sand">Make an evening of it</p><h2 className="display text-6xl leading-[.85] sm:text-7xl">RESERVE<br/>YOUR TABLE</h2><p dir="rtl" className="mt-8 max-w-sm text-right text-sm leading-8 text-white/75">השאירו פרטים ואנחנו נדאג לשמור לכם מקום מול הים.</p></div><div>{sent ? <motion.div initial={{opacity:0,y:15}} animate={{opacity:1,y:0}} className="border border-white/35 p-9 text-center"><p className="display text-4xl">Thank you.</p><p dir="rtl" className="mt-3 text-sm text-white/80">בקשתכם התקבלה. צוות NOIR יצור עמכם קשר בהקדם.</p><button className="mt-7 text-xs font-bold tracking-[.15em] underline" onClick={()=>setSent(false)}>NEW REQUEST</button></motion.div> : <form onSubmit={submit} dir="rtl" className="grid grid-cols-2 gap-x-4 gap-y-6">{[['name','שם','text'],['phone','טלפון','tel'],['date','תאריך','date'],['time','שעה','time']].map(([id,label,type])=><label key={id} className="col-span-2 text-right text-[10px] font-bold tracking-[.15em] sm:col-span-1">{label}<input required type={type} name={id} className="mt-2 w-full border-b border-white/50 bg-transparent px-0 py-3 text-sm outline-none transition focus:border-sand"/></label>)}<label className="col-span-2 text-right text-[10px] font-bold tracking-[.15em]">מספר סועדים<select required defaultValue="" name="guests" className="mt-2 w-full border-b border-white/50 bg-ocean px-0 py-3 text-sm outline-none focus:border-sand"><option value="" disabled>בחרו מספר סועדים</option>{[1,2,3,4,5,6,7,8].map(n=><option key={n}>{n}</option>)}</select></label><label className="col-span-2 text-right text-[10px] font-bold tracking-[.15em]">הערה<textarea name="note" rows={2} className="mt-2 w-full resize-none border-b border-white/50 bg-transparent px-0 py-3 text-sm outline-none focus:border-sand"/></label><button className="button col-span-2 mt-3 border-white text-white hover:bg-white hover:text-ink" type="submit">Request a table</button></form>}</div></div></section>}
